{
  "name": "fupa-snack-cron",
  "version": "1.0.0",
  "description": "Cron jobs for Fupa Snack System",
  "main": "cron.js",
  "scripts": {
    "start": "node cron.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "node-cron": "^3.0.2"
  },
  "engines": {
    "node": "18.x"
  }
}